*Question B: "Would I have FOUND combo X without looking at the future?"** (Overfitting detection)
→ This is where WFO matters. If you run 500 combos on 2 years and pick the best,
   the question is: would the same combo also be the best if you only had 18 months?
   If different combos win on different sub-periods, your "best" is just the one that
   happened to fit the full period's quirks.

when you say:
"If you run 500 combos on 2 years and pick the best,
   the question is: would the same combo also be the best if you only had 18 months?"
How do you define "the best"?
is that the combo with the best sharpe result? no.. I'm ALMOST 100% sure every period would give best sharpe values to different combos, if you ask that!
And let's use for example 500 combos over 2y, we found that all 500 combos had >=100 orders and 30 combos had amazing sharpe, 100 combos had average but still good sharpe. and we want to use WFO. How would WFO help us here? do we check which combos were the most profitable on each "slice" of WFO? but what do we use with "training" period and with "testing" period if we do use grid combos? I understand that different periods would give better sharpe ratio for different combos, that's I got, but what is training and what is testing periods for with grid search? and what do we do those best share grid combos (let's say all 500 params combos we have on each period sorted by sharpe ratio) - how do we decide which combos are robust which are not? and how do we use WFO in this case?

What if we do grid search on each combo, but we track in result not just sharpe for each combo, but CompositeScore and all it's inner parts (CompositeComponents):
struct CompositeComponents
{
    double sharpe = 0, calmar = 0, profitFactor = 0, plateau = 0;
    double trades = 0, costStress = 0, winRate = 0, ddPenalty = 0;
};
and also we have:
struct RobustResult
{
    CombinedParams params;
    const char* exitMode;      // strategy exit mode description
    double sharpe;
    double sortino;
    double calmar;
    double totalReturn;
    double maxDrawdownPct;
    double winRate;
    double profitFactor;
    size_t totalTrades;
    double plateauRatio;
    double avgNeighborSharpe;
    int neighborCount;
    double compositeScore;   // Trader7-style composite
    CompositeComponents cc;  // score breakdown
    double wfoPassRate;      // -1 if WFO not run
    double wfoAvgTestSharpe;
};

do we have EVERYTHING we need here for ALL workflows you listed in vendor/flox/_my/crush-glm/docs-ai/WFO_AND_ALPHA_WORKFLOWS.md or do we need anything else to have before we are sure we can try any of the workflows you provided?

How exactly we are looking for plateaus? do we do all gird combos and find their RobustResult and then we keep in all grid combos all params "frozen" beside two params which become x and y axis and we check what would be the sharp (or any other important result value) for each of those two "unfrozen" params values?

```c++
// Trader7-style composite score: Sharpe is only 25% of the total.
// Prioritizes stable plateaus over high-Sharpe spikes.
// Fills CompositeComponents with each weighted sub-score for reporting.
inline double computeCompositeScore(double sharpe, double calmar, double profitFactor,
                                     double plateauRatio, double winRate, size_t trades,
                                     double maxDrawdownPct, double costStressSharpe,
                                     CompositeComponents& cc)
{
    // Normalize each component using tanh to bound [0, 1]
    auto norm = [](double v, double scale) { return std::tanh(std::max(v, 0.0) / scale); };

    double nSharpe     = norm(sharpe, 3.0);       // tanh(sharpe/3): 3.0 → 0.76, 6.0 → 0.96
    double safeCalmar  = std::isnan(calmar) || std::isinf(calmar) ? 0.0 : calmar;
    double nCalmar     = norm(safeCalmar, 10.0);   // Calmar = return/maxDD
    double safePF      = std::isnan(profitFactor) || std::isinf(profitFactor) ? 0.0 : profitFactor;
    double nPF         = norm(safePF - 1.0, 1.5); // tanh((pf-1)/1.5): 2.5 → 0.76
    double nPlateau    = std::min(std::max(plateauRatio, 0.0), 1.0);
    double nTrades     = norm(static_cast<double>(trades), 150.0); // tanh(trades/150)
    double nCost       = norm(costStressSharpe, 2.0);  // Sharpe after 2x fees
    double nWinRate    = std::min(std::max(winRate - 0.35, 0.0) / 0.45, 1.0); // 35-80% → 0-1

    // Drawdown penalty (like Trader7: max(0, dd - 35%))
    double ddPenalty = std::max(0.0, std::abs(maxDrawdownPct) - 35.0) / 100.0;

    // Composite: 25% sharpe + 20% calmar + 15% PF + 15% plateau + 10% trades + 10% cost + 5% winrate - penalties
    double score = 0.25 * nSharpe
                 + 0.20 * nCalmar
                 + 0.15 * nPF
                 + 0.15 * nPlateau
                 + 0.10 * nTrades
                 + 0.10 * nCost
                 + 0.05 * nWinRate
                 - ddPenalty;

    // Fill component breakdown (weighted values)
    cc.sharpe      = 0.25 * nSharpe;
    cc.calmar      = 0.20 * nCalmar;
    cc.profitFactor = 0.15 * nPF;
    cc.plateau     = 0.15 * nPlateau;
    cc.trades      = 0.10 * nTrades;
    cc.costStress  = 0.10 * nCost;
    cc.winRate     = 0.05 * nWinRate;
    cc.ddPenalty   = ddPenalty;

    return score;
}
```























----------















algo trading. please explain me best practices of how WFO (walk forward) is done. it's concept, best practices. how many folds should we use. how to avoid lookahead biases and other issues. which common bugs happen and how to avoid it. how should ideal WFO look like. Why beside WFO quants recommend also to do Monte Carlo, Roman Woflf, DSO/DSR, and what else and why? explain all robustness teqniques. why WFO is not enough? Right now I do WFO on past 2 years data on H4 timeframe with 5 folds. train bars: 613 test bars: 239 periods: 2024-05-09 - 2024-08-19 (train) / 2024-08-23 - 2024-10-02 (test) 2024-10-02 - 2025-01-12 (train) / 2025-01-16 - 2025-02-25 (test) 2025-02-25 - 2025-06-07 (train) / 2025-06-11 - 2025-07-21 (test) 2025-07-21 - 2025-10-31 (train) / 2025-11-04 - 2025-12-14 (test) 2025-12-14 - 2026-03-26 (train) / 2026-03-30 - 2026-05-09 (test) My trasholds: OOS >= 5 trades, IS >= 20 trades. - is that good trasholds? and even with this strasholds, almost all of them fail because of insuficient trades count! which issues do you see? should we do less folds than 5? how many folds minimum needed? I understand between train and test on the same fold we must make some gap in time to avoid lookahead bias BUT do we need the next fold's training period start to be the end of the previous fold end of test period? if we do 5 folds on 2 years, the periods become very small and we don't have enough trades to do WFO correctly! is it allowed (and if it's a good practice) that we would still do 5 folds, but we change the training periods and testing periods in a way that next folds training period is, let's say, in the middle of the previous folds training period, but testing period NEVER overlaps with the previous testing period? maybe something different, but you got the idea, my idea is to allow much MORE trades to be inside the folding period! or make less folding periods - and here I'm not sure what is better and what is worse. Do you understand what I want to achieve? Please give me EVERYTHING I should know about WFO and other robustness and proving techniques for trading strategies!

















I do grid search. Like I define coarse grid params for the strategy and try all possible combos, then I track the results. Would I even benefit from WFO at all? I train on the whole period but ALL combinations! does it change anything? My idea that if I test all combos on all params, then I can analyze the params for plateaus, right? but how do I analyze params if my search grid can have like 4-5 params with 5-6 values in each param. Once I tested all params, should I do "2D" slices (like if we have 5 params, then we fix 3 params values and change other 2 params values to build from one param values columns, from other rows and values in cells would be what we optimize for (sharpe for example) - correct? then we check the neighbors cells and if the optimization value (like sharpe) is similar among those neighrobrs it means we have plateau. Is that correct way of finding plateau? it it more reliable than WFO? BTW, when we test all combos over all 2y period, we alrady have the trade start/end date and we can figure out which periods (per year, per quater, per month) we had how many trades and which sharpe in each particular period of  (per year, per quater, per month)  for each combo, right? would that two things (2D slices for whole period and per year/quoter/month) be enough for robustnes? or do you think even found plateau on 2D params can be "luck"? and if we also do check the results not only for whole 2y but also per year/quoter/month and they are similar during all periods - is that not yet enough evidence that the strategy is working? also we are testing ALL the same params combos not only on one coin, but on 40 coins. and if we get similar results at least on 3 coins from those 40 coins on the same params combo (and similar plateus optinally) - is not hat enough evidence? how is it called correctly what I'm trying to do? is that correct way of finding plateaus and is what I described a correct way to find robust alphas? also I would see if results degrade each quater from 2y ago till the latest quater and I would give more wight to the most recent quater results. and the fact that we have time slices and we have all params combos tested and we have results for all coarse grid params results give us ability to build 2D params slices. Is that good approach? how do make correctly what I am descriing? which additional proves and robustness do we need to use with my approach?

also which params can we optimize for? what are available params for optimization? is it only sharpe ratio? what else? from what I know we have: PnL, sharpe, calmar, profitFactor, plateauRatio, avgNeighborSharpe, trades, winRate, drawdawn, sortino? which other params can we track? which we should track? on which params is it possible to optimize for? how do we optimize, just sort params combos by that optimization param value? should we sort by one param value or by several params values? or should we use some "custom composite score" which is like a  formula (for example like: Composite: 25% sharpe + 20% calmar + 15% PF + 15% plateau + 10% trades + 10% cost + 5% winrate - penalties). I need to know everything with your pros/cons and suggestions.

How exactly we are looking for plateaus? do we do all gird combos and find their RobustResult and then we keep in all grid combos all params "frozen" beside two params which become x and y axis and we check what would be the sharp (or any other important result value) for each of those two "unfrozen" params values?

Also, what are the real world quant alpha search workflows? do they use WFO with just random params? but how then they

find robust params and not just spikes? or do they use grid search? but then why WFO matters that match if pro quants

anyways tried all combos of the grid? do quants use optuna params optimization? if they do use optuna, how do they make

sure they find robust plateaus and not just noise spikes? or do you want to say pro quants look for optuna spikes and

then each spike they pass through WFO? but that's too much calculating the noise... so what are the real workflows? give

me ALL the best workflows to find alphas sorted from the most promissing and let me know why each approach works, it's

pros/cons and when it's better then others. I need a full complete list with all suitable robust alpha finding workflows which really works.































---



"Full Grid Search (2Y)" BUT I do full 2y and then check each period split (each year, each quoter, each month) to get it's own metrics by "sum up" trades started and ended in that particular period, not only 2y total.  I can even run separate backtest on each year, each quoter, each month separately (only if you confirm it must be done like that and that one search over 2y and then splitting with sum up does not work. Does it work or not?
Remember: I do grid search of all parameters combos! it means I do not need to "train" on some period, because coarse grid search would ANYWAYS test each combo on each period! so (as I think) "training" like what WFO is doing has no sense because I would anyways test each and every combo on each period! Do you see what I mean? I don't get what would be "training" meaning if I test each combo on each period anyways...


When you say: "Testing all combos on 2 years increases overfitting risk, not decreases it. You're implicitly running thousands of trials and picking the best.", how do you define "picking the best"?
BUT, it depends on what do you mean with "picking the best"! what do you actually mean? please clarify. Because for me "picking the best" means picking the most robust parameters plateaus which have reasonably good results (combination of sharpe, stability, plateau ratio, risk, draw down rate). Key thing is "robust parameters plateau", not just picks/needles with best sharpe ratio! so "killing fragile parameters" would be done with my corse grid params combos testing ANYWAYS in my backtest! Did you take that into consideration in your previous reply? So, do I stil benefit from WFO in that case or does it become more or less useless?

---

With "Correct Plateau Detection Algorithm":

```
### ✅ Correct Plateau Detection Algorithm:
1. **Normalize parameter space** so each dimension has equal scale (e.g., min-max or z-score).
2. For each combo `i`, define a neighborhood radius `r` (e.g., ±1 grid step in each dimension).
3. Collect all combos `j` within distance `r` (Manhattan or Euclidean in normalized space).
4. Compute:
   - `μ_neighbor = mean(metric_j)`
   - `σ_neighbor = std(metric_j)`
   - `PlateauScore_i = μ_neighbor - λ * σ_neighbor` (λ ≈ 0.5-1.0)
   - Or: `RobustnessRatio_i = μ_neighbor / (σ_neighbor + ε)`
5. **High PlateauScore / Low σ_neighbor = flat, robust region. Sharp peak = high σ_neighbor = curve-fit.**

This works in any dimension. You don't need 2D slices. You compute neighborhood stability directly in N-D space.

**Implementation tip:** Store all grid results in a DataFrame. Use `scipy.spatial.KDTree` or `sklearn.neighbors` to query neighbors efficiently. Compute plateau metrics vectorized.
```

What does "Parameters interact non-linearly" and what does "parameter independence" mean? please give me some examples with visualization so I can explain it to newbies in algo trading.

Can you give me python code example of how to do that on (select one most reasonable from below):

1) donchian
PARAM_GRID = {
    # Entry channel: how many bars define the breakout level
    # Lower = more reactive, higher = more selective
    "channel_period": [10, 15, 20, 30, 40, 55],

    # Exit channel: shorter midline for profit-protection exit
    # Rule: must be < channel_period (CONSTRAINTS enforces this)
    "exit_period": [5, 8, 10, 15, 20],

    # Supporting alpha: volume surge multiplier (0 = disabled)
    "vol_mult": [0.0, 1.3, 1.5, 2.0],
}

2) keltner_breakout
PARAM_GRID = {
    "ema_period":    [10, 20, 30, 40, 50, 60, 80],  # Slower than altcoin grid
    "atr_period":   [10, 14, 20, 30],
    "atr_mult":     [2.0, 2.5,2.8, 3.0, 3.2, 3.5],     # Slightly wider
    "atr_pct_min":  [0.0, 0.005, 0.008],
}

3) keltner_squeeze
PARAM_GRID = {
    "ema_period": [10, 20, 30],
    "kelt_mult":  [1.5, 2.0, 2.5],
    "bb_period":  [10, 20, 30],
    "bb_std":     [1.5, 2.0, 2.5],
}

4) macd
PARAM_GRID = {
    "fast":          [8, 12, 16],
    "slow":          [21, 26, 34],
    "signal_period": [7, 9, 12],
    # trend_period: supporting — EMA trend filter (0=disabled)
    "trend_period":  [0, 100, 200],
}

5) rsi_bb_mr
PARAM_GRID = {
    # RSI period — lower = faster oscillation = more signals
    "rsi_period": [7, 14, 21],
    # RSI oversold/overbought thresholds
    "rsi_low":    [20, 25, 30],
    "rsi_high":   [70, 75, 80],
    # BB parameters — tighter std = more selective entry
    "bb_period":  [15, 20, 30],
    "bb_std":     [1.5, 2.0, 2.5],
}

6) rsi2
PARAM_GRID = {
    # RSI period: 2 is Connors classic, 3 is slightly smoother
    "rsi_period":   [2, 3],
    # entry thresholds: when RSI is below/above these → enter
    "entry_low":    [5, 10, 15],
    "entry_high":   [85, 90, 95],
    # trend filter period: only trade in direction of this SMA
    "trend_period": [50, 100, 200],
}

7) trend_pullback
PARAM_GRID = {
    # Fast EMA defines the pullback/reclaim line.
    "fast_ema": [10, 20, 30],
    # Slow EMA defines the dominant trend; fast must stay below slow.
    "slow_ema": [50, 100, 150, 200],
    # RSI threshold marks a long pullback inside an uptrend.
    "rsi_pullback": [35, 40, 45],
    # ATR% filter avoids dead, low-movement periods where fees dominate.
    "atr_pct_min": [0.0, 0.004, 0.008],
}

8) vol_compression_breakout
PARAM_GRID = {
    # Range window whose high/low define the breakout boundary.
    "range_window": [20, 30, 40, 60],
    # Lookback for ranking ATR% compression.
    "compression_window": [60, 100, 150],
    # Compression percentile; lower means stricter squeeze requirement.
    "compression_pct": [0.2, 0.3, 0.4],
    # Volume confirmation reduces false breakouts from thin liquidity.
    "vol_mult": [0.0, 1.3, 1.5],
}

9) vsa
PARAM_GRID = {
    "vol_window": [20, 30, 50],
    "spread_mult_high": [1.5, 2.0],
    "spread_mult_low": [0.5, 0.7],
    "vol_mult_climax": [2.5, 3.0],
}

also give me data type of `scipy.spatial.KDTree`, `sklearn.neighbors` and how to use them. Is it possible to visualize them?

Give me code examples in Python and in C++ (I gues C++ does not have that rich libraries as Python has, or does C++ have them as well)? I do my backtests in both Python and in C++. C++ has much better speed and Python has much better libraries support (I gues so) but is much slower.

How "Correct Plateau Detection Algorithm" is different from "parameters exploration" that I had in mind initially?

---

What does "log(Trades)" mean? how to caluclate what to store?

---

I use Brute (Coarse) Grid and not optuna, because optuna may miss something (some params combos) and coarse grid tests all combos. That make me think that Coarse Grid is better than Optuna. Am I wrong?

---

Cluster 40 coins by correlation (e.g., hierarchical clustering, cut at 0.7).
How to find coins correlation? I was going to do it because not always on H4 I can get enough trades to analyze, that's why I was thinking on making buckets of correlated coins, just to get more trades for analysis. Is that a good idea? But how to find the correlations?

---

When you say "Step 3: Temporal Validation (Anchored WFO)", how do we do WFO if we test the same params combo? I ask because I'm not sure how do we do "training" because we are not looking for "best params" we have those params known, we just test for robustnes, so how "traning" period is applied and used in our case? What do I miss?

---

Is 2D slicing the correct way to find plateaus?
No. It's a visualization tool. Use N-D neighborhood metrics (NeighborStd, PlateauScore) directly in normalized parameter space.
Q: and how do we visualize N-D neighborhood metrics? should we?

---

What is my approach called?
Parameter Space Mapping + Cross-Sectional Exploration. Valid for research, insufficient for validation.
Q: yes, for sure it's research only, the validation is what I'm looking for. Actually I'm looking for the best possible Research + Validation approaches (Real-World Quant Alpha Discovery Workflows). And I had a feeling that Coarse Grid is the only valid way to do the research because Optuna would defenitelly miss some combos and Coarse Grid is superior to Optuna approach for research, is that true? What are the best ways for research only? Can we try different research approaches (which exist, give me ranked) and then try different validation approaches (which exist, give me ranked)? and can we combine different research approaches with different validation approaches? As I undertand, does not matter what I use for research (corase grid, optuna, etc...) we still apply the same validation approches after, right? 

---

is WFO a research method or validation method only? I ask because of "training" period makes me think that we look for best parameters combo during training and then we do OOS and then we do traning again and compare if previous training params combos are still good on the next training and next OOS. Is that the main reason of applying and testing with WFO, or do I miss something?

---

Regime detection and labeling of each candle? How would it help us? that we can check how many candles of which regime was during particular time period (year, quater, month)? should we apply "confindence" score for each regime while labeling each candle?

---

I use FLOX C++ library, it's a custom library which allows to perform very fast grid backtesting and also supports trading from the same C++ strategy. It works much faster than even Vectorbt! But it works fast because it backtests all grid params inside C++.
How can I use my custom FLOX C++ backtesting app with Optuna? Can Optuna work with bulk backtesting results for the whole grid params results or does optuna need to call each backtest individually (which would be MUCH slower than the whole bulk grid params results obtained in one C++ app call)?

---

You gave me these 3 alpha search workflows:


### 🥇 1. Hypothesis-Driven + Constrained Bayesian/Optuna + Purged CV + DSR/FDR
- **How it works:** Start with economic rationale → define 3-4 params → Optuna with composite objective + hard constraints → Purged/Embargoed CV or CPCV → DSR adjustment → FDR correction across assets → Monte Carlo → Forward test.
- **Pros:** Minimizes trials, penalizes overfitting natively, statistically rigorous, industry standard for ML/systematic.
- **Cons:** Requires coding discipline, understanding of statistical corrections.
- **When to use:** Any serious alpha research. Best for medium/low frequency, multi-asset, or ML-augmented strategies.

### 🥈 2. Anchored WFO + Parameter Stability Scoring + Monte Carlo + Forward Test
- **How it works:** Grid/Bayesian search on IS → filter by composite score → run 3-4 fold anchored WFO → concatenate OOS → run 5k-10k Monte Carlo paths → check DSR/PSR → 3-6M paper → live.
- **Pros:** Simulates real deployment, tests temporal stability, easy to implement, robust for rule-based strategies.
- **Cons:** Computationally heavier than single split, requires sufficient data/trade count.
- **When to use:** Parametric strategies, trend/mean-reversion, crypto/futures, when you re-optimize quarterly.

### 🥉 3. Cross-Regime/Cross-Asset Validation + Composite Robustness + Time-Decay OOS
- **How it works:** Test survivors across volatility/trend regimes → group assets by correlation → apply FDR → weight recent OOS higher but enforce trade minimums → Monte Carlo execution stress.
- **Pros:** Captures regime fragility, realistic for crypto/macro, handles multi-asset universes well.
- **Cons:** Regime classification is subjective, requires careful correlation clustering.
- **When to use:** Multi-coin crypto, macro strategies, when markets exhibit clear regime shifts.


Beside your alpha search workflows I have found these 7 workflows (are they worse than what you gave me? do they make sense? are some of those 7 workflows better than what you provided? why?):


### Workflow 1: Grid Search + Overfitting Defense (Most Robust)

**What it is**: Exhaustive grid → CSCV/PBO overfitting check → plateau detection → stress testing.

**How it works**:
1. Define parameter bounds based on domain knowledge
2. Run dense grid (500-5000 combos)
3. For each combo: compute Sharpe, total return, max DD, trade count
4. Compute PBO via CSCV: randomly split data into S subsets, train on all combinations
   of S-1 subsets, test on the held-out one. PBO = probability the best IS combo is
   also best OOS. High PBO → overfitting.
5. Plateau detection: the best combo's neighbors should also score well. If only one
   isolated point scores well → spike (noise). If a flat region scores well → plateau (signal).
6. Stress test: add slippage, fees, random delays. Does the edge survive?

**Pros**:
- Catches overfitting directly (PBO measures it)
- Plateau detection separates signal from noise
- No "training" in the traditional sense — you test everything
- Works well with moderate parameter spaces (2-4 params)

**Cons**:
- Computationally expensive for large grids
- PBO assumes stationarity of the data splits
- Doesn't adapt to regime changes

**When to use**: Default approach. Use this when you have 2-5 parameters and domain
knowledge about reasonable ranges.

**Who uses this**: Most systematic trading firms. This is the standard workflow for
parameterized strategies.


### Workflow 2: Optuna/TPE + Stability Probing

**What it is**: Smart search (not exhaustive) + perturbation stability + plateau validation.

**How it works**:
1. Use Optuna's TPE sampler to explore parameter space efficiently
2. Each trial gets a Sharpe score from a backtest
3. Top 50 trials get perturbation tests: ±5% on each parameter, re-run
4. Low variance across perturbations → genuine plateau
5. High variance → spike, discard
6. Extract KDE (kernel density estimate) of top-performing regions
7. Run dense grid ONLY in the identified plateau regions

**Pros**:
- 10-50x faster than full grid for high-dimensional spaces
- Natural plateau detection through perturbation
- Can handle 5-10 parameters efficiently
- KDE zone extraction finds multi-modal regions

**Cons**:
- TPE is exploitation-focused — may miss distant plateaus
- Needs enough initial random trials to seed properly
- Perturbation stability doesn't catch all forms of overfitting

**When to use**: High-dimensional parameter spaces (5+ params), limited compute,
or as a pre-filter before dense grid.

**Who uses this**: Optuna is used by JP Morgan, Nomura, and many crypto quant shops.
The perturbation approach is from Bailey et al. "The Probability of Backtest Overfitting".

**This is what Trader7's RANGEFIND phase does.**


### Workflow 3: Walk-Forward Optimization (Traditional)

**What it is**: Sequential train/test splits with rolling windows.

**How it works**:
1. Split data into N folds (e.g., 5 folds of 5 months each)
2. For each fold: train on the fold (find best params), test on the next fold
3. Average OOS performance → estimate of live performance
4. OOS/IS ratio < 0.6 → overfit

**Pros**:
- Directly answers "would this have worked in real-time?"
- Models the actual deployment workflow (optimize → deploy → re-optimize)
- Simple to understand and implement

**Cons**:
- Only uses each data point once for OOS → high variance in estimate
- Sensitive to fold boundaries (a lucky boundary can inflate results)
- "Training" in WFO is often just finding the best single combo → same overfitting risk
- Low statistical power for short datasets

**When to use**: When you want to simulate a realistic deployment schedule. Good for
strategies that you plan to re-optimize monthly/quarterly.

**Who uses this**: Retail traders, some fund desks that re-optimize on a schedule.
Less common in sophisticated shops — CPCV (Workflow 4) is preferred.


### Workflow 4: Combinatorial Purged Cross-Validation (CPCV)

**What it is**: López de Prado's method. All possible train/test splits with purging.

**How it works**:
1. Split data into N groups (e.g., 8 groups of 3 months)
2. For each combination of choosing k groups for training (C(N,k) combinations):
   - Train on k groups → find best params
   - Test on remaining N-k groups
3. Average across ALL combinations → unbiased performance estimate
4. "Purge" the gap between train and test to prevent leakage

**Pros**:
- Uses all data for both training and testing (every point is OOS in some splits)
- Much lower variance than traditional WFO
- Purging prevents information leakage across train/test boundaries
- Theoretically sound (comes with mathematical guarantees)

**Cons**:
- Computationally brutal: C(16,8) = 12,870 combinations
- Each combination requires a full optimization → very expensive
- Complex to implement correctly (purging, embargo)
- Overkill for small parameter spaces

**When to use**: When you have lots of compute and need the most rigorous overfitting
test possible. Gold standard for publication or regulatory review.

**Who uses this**: Quant funds following López de Prado's "Advances in Financial Machine
Learning". Initially developed for Abeomics/QuantConnect.


### Workflow 5: Rolling Ensemble (Adaptive)

**What it is**: Don't pick one best combo — use an ensemble that adapts over time.

**How it works**:
1. Run full grid on trailing 12-month window
2. Take top-5 combos (not just the best)
3. Average their signals (ensemble)
4. Every month: re-run grid on the new trailing 12-month window
5. Update ensemble weights (more recent performance → higher weight)

**Pros**:
- Adapts to regime changes naturally
- Ensemble is more robust than any single combo
- Recent data gets more weight (your preference)
- Reduces the "which combo to pick" problem

**Cons**:
- Always slightly behind the current regime (trailing window)
- Higher turnover (ensemble changes monthly)
- More complex to implement and monitor
- Can ensemble together combos that contradict each other

**When to use**: Live trading with regular re-optimization. Best when you believe
regimes change every 6-18 months.

**Who uses this**: Many CTAs and systematic macro funds. AQR's style factors use
rolling optimization windows.


### Workflow 6: Monte Carlo Stress Testing

**What it is**: Don't test different params — test different realities.

**How it works**:
1. Find best combo on actual data
2. Randomly shuffle trade returns 1000 times
3. Check: is 5th-percentile PnL still positive?
4. Add random slippage delays (0-3 bars)
5. Test with 2x, 3x, 5x fees
6. Check: does the edge survive 95% of alternate realities?

**Pros**:
- Tests robustness without needing different params
- Catches "lucky trade ordering" (same trades, different sequence)
- Simple to implement and interpret
- Works with any optimization method

**Cons**:
- Assumes trade returns are i.i.d. (they're often autocorrelated)
- Doesn't catch overfitting in parameter selection
- Slippage simulation is crude (real slippage is path-dependent)

**When to use**: As a final validation step after any optimization method. Always
run this before deploying.

**Who uses this**: Everyone. This is table stakes.


### Workflow 7: Pure Signal Research (No Optimization)

**What it is**: Don't optimize parameters at all — use economic rationale.

**How it works**:
1. Identify a market inefficiency through domain knowledge
2. Design a signal that exploits it with fixed, reasoned parameters
3. Backtest once. If it works → deploy. If not → rethink the thesis.
4. No parameter search → zero overfitting risk

**Pros**:
- Zero overfitting risk (no optimization = no multiple testing)
- Economically grounded (you understand WHY it works)
- Simple, fast to implement
- Parameters have economic meaning (e.g., 200-day MA because institutions use it)

**Cons**:
- Requires deep market understanding
- You might miss better parameter values
- Very few genuine edges are discoverable this way
- Confirmation bias risk (you rationalize why it should work)

**When to use**: When you have genuine market insight. Best for fundamentally-driven
strategies (carry, value, momentum).

**Who uses this**: Renaissance (reportedly), some discretionary quant funds, academic
researchers testing specific hypotheses.

Note:

Most quant shops use **Workflow 1 (Grid + Overfitting Defense)** as the primary method,
with **Workflow 6 (Monte Carlo)** as validation. Here's the typical pipeline:

```
1. Domain research → identify candidate strategy
2. GRID SEARCH on 2-4 parameters (100-2000 combos)
3. CSCV/PBO check → discard if PBO > threshold
4. Plateau detection → discard if isolated spike
5. Slippage stress test → discard if edge vanishes with realistic costs
6. Monte Carlo shuffle → discard if 5th-pctile PnL < 0
7. Paper trade 30-90 days → discard if live ≠ backtest
8. Deploy with circuit breakers
```

---

can cpp produce all metrics?
DSR, Calmar, PF, Trades, MaxDD, NeighborStd, PlateauScore, DSR/FDR, log(Trades), Sharpe, Sortino, Calmar, DSR, PSR, Profit Factor, Expectancy, Avg Trade, Cost-Adjusted Return, Max DD, Ulcer Index, Rolling Vol, Trade Count, Turnover, PlateauScore, NeighborStd, ParamSensitivity, RegimeConsistency, PBO, CSCV, KDE (kernel density estimate) of top-performing regions,
CPCV, Monte Carlo path testing
Romano-Wolf, Benjamini-Hochberg FDR, 

---

I attached my C++ code (current project) and my old Trader7 robustnes check project files for you to analyze for bugs, ideas, insights and suggetions, so you could understand what I was doing and what I am doing now.
































---


"WFO is not for finding parameters. WFO simulates forward deployment. You take your top 5-10 grid survivors and ask: "If I had locked these params in 2024, would they have survived 2025 unseen data?" The "IS" in WFO with fixed params is just a consistency baseline, not an optimizer."
Q: yes, I agree. but when we have grid params, and we got those params combos not from results after backtesting on 2y but we got our grid params from business knowledge  and we test the same params (and those params combos know NOTHING about the 2y period) and we run backtests on individual periods like 2022, 2023, 2024, 2025, 2026 separately, is not that the same as we backtesting simulating forward deployment? and even more than that, I am not taking top 5-10 grid survivors, I'm backtesting ALL combos!!! on each period and checking AUTOMATICALLY which params combos survived on each period! as for me, I'm doing exactly what you said WFO is for, but I'm doing that not with just top 5-10 grid combos, but on ALL combos and on ALL periods AUTOMATICALLY! what do I miss thinking that it's the same as WFO, but just even more superior to WFO? forget about  summing trades into calendar buckets, what if I run on each calendar period INDIVIDUALY? that's what I also asked you if there is a difference if I backtest on each period separately VS summing up. Would it be the same what you called WFO just not on top 5-10 grid combos, but on ALL combos? that's actually what I asked and what I assumed if we do then we do not need another WFO just on top 5-10 grid combos, if we already doing it on ALL combos and you replied me "false"


---

"Temporal OOS (WFO/CPCV)
Multiple testing correction (DSR/PBO/FDR)
Path dependency stress (Monte Carlo)"
I DO agree we need to do "DSR/PBO/FDR" and Monte Carlo! I just not sure why do we need to do WFO if we already tested ALL grid combos on all possible periods (each year individually, each quater individually and maybe each month individuslly), NOT summing up, but running separate backtest on particular period on each grid combo! is not that what you call WFO, but just on ALL grid combos?

---

DSR/PBO/FDR, Monte Carlo - I need examples in C++ and Python of how to do it properly and C++ (I really hope) would do the calculation SUPER fast, so we can run it much faster than we would do in Python. But Python code I also need (beside C++ main priority code) just for parity, and I still would run some strategies in Python after coarse Vectorbt grid combos backtest (again, just for parity) to double check if I had no stupid (or not obvious) mistakes/bugs in my main C++ code

WFO/CPCV - I also need code for it, but I'm just not sure we we are not doing the same tests which we have done for all grid combos, but this time we do it just on top 10-14 combos... that's my main concern.

---

nanoflann - amazing library! does it allow to work with any count of params? how slow would it be if our strategy would have not just 3-4 params, but more? What if we have 5D, 6D, 7D? or should we ALWAYS avoid more than 4 params in the strategy? What are the correct approaches?

---

log(Trades) = Just std::log(trades) or np.log(trades).
Ah... with "trades" you mean trades_count, not array of trades structs!
I thought you meant some special function on array of complex trade structures/objects.

---

Please provide C++ formulas for all of:
DSR: Φ((SR_obs - E_max_SR) * sqrt(N-1) / σ_SR) → <cmath>, <numeric>
PBO/CSCV: Combinatorial splits, rank IS winner in OOS, logit transform → <algorithm>, <vector>
FDR/Romano-Wolf: Sort p-values, stepdown adjustment → <algorithm>
Monte Carlo: Block bootstrap, percentile calculation → <random>, <numeric>
Calmar/Sortino/Ulcer/Rolling Vol: Standard math → <cmath>, <deque>
and all other metrics!
are there C++ libraries which may have it, or is it faster to implement by ourself? just make sure you use the self-explanatory param names and also use comments explaining what is going on in those functions.
Which other params and functions for them may we need in our alpha hunting research? I'm pretty sure we still miss some...


---

Regime labeling:
trending_bull, ranging, high_vol, crash
I guess you oversimplified.

I had in mind this regime axis and regimes for each axis:
direction: bear/bull
momentum: negative, positive, strong negative, strong postive, weak
trend strength: choppy, parabolic, strong trend, weak trend
volatility level: extreme, high, low, normal
volatility regime: contraction, expansion, normal

or is that useless / overcomplicating / missing something more important?


---

Multi-Asset Correlation & Regime Labeling
You missed C++ version!
and what do I need to know about coins in order to calculate the correlations? and in which "dimensions" do we calculate the correlations?

---

You have seen my C++ code, and you seen I'm testing each coin individually. How to backtest "clusters" of coins and not each coin individually? As I understand that have 2 main goals: 
1) have more trades for analysis than testing each coin individually 
2) have less tests than each coin individuslly. 
right? what else?

---

Which result metrics do we track per whole grids experiments in total (overall)? 
which result metrics do we track per each grid combo?
which metrics do we track per for all parameters? 
which metrics do we track per each parameter? 
which result metrics do we track per parameters combinations (1D, 2D, 3D) (with abilit to use them also for SVG visualization)? 

Which metrics should we track with "range/count distribution" like:
"mfe_range_count_distribution": {
   "le_0.0": 0,
   "0.0_to_0.01": 18,
   "0.01_to_0.03": 23,
   "0.03_to_0.05": 18,
   "0.05_to_0.10": 17,
   "gt_0.10": 13
   },
"mae_range_count_distribution": {
   "lt_-0.10": 2,
   "-0.10_to_-0.05": 13,
   "-0.05_to_-0.03": 21,
   "-0.03_to_-0.01": 35,
   "-0.01_to_0.0": 18,
   "ge_0.0": 0
   },
"r_range_count_distribution": {
   "<-2R": 10,
   "-2--1R": 22,
   "-1-0R": 18,
   "0-1R": 9,
   "1-2R": 10,
   "2-5R": 9,
   "5R+": 11
   },

Which metrics (mabye all) should we track with what I call "canonical distribution" like:

```py
def distribution_stats(series: pd.Series | list | tuple | np.ndarray, *, include_sum: bool = False, unit: str | None = None) -> dict:
    """Return the canonical distribution schema for numeric values.

    Empty inputs still return every required key with `None`, except
    `count == 0`. This makes downstream AI and chart code deterministic: a
    missing metric is visible as a null value, not a missing property.
    """
    numeric = pd.to_numeric(pd.Series(series), errors="coerce").replace([np.inf, -np.inf], np.nan).dropna()
    if numeric.empty:
        out = {field: None for field in CANONICAL_DISTRIBUTION_FIELDS}
        out["count"] = 0
        if include_sum:
            out["sum"] = 0.0
        if unit is not None:
            out["unit"] = unit
        return out

    sorted_values = numeric.sort_values()

    def q(level: float) -> float:
        """Return one quantile as a JSON-safe float."""
        return float(sorted_values.quantile(level))

    def top_tail_mean(fraction: float) -> float:
        """Return the mean of the top fraction of values, with at least one row."""
        n = max(1, int(math.ceil(len(sorted_values) * fraction)))
        return float(sorted_values.tail(n).mean())

    p25 = q(0.25)
    p50 = q(0.50)
    p75 = q(0.75)
    max_value = float(sorted_values.max())
    out = {
        "count": int(len(sorted_values)),
        "mean": float(sorted_values.mean()),
        "median": p50,
        # Use sample standard deviation for small research slices. Most
        # neighbourhoods and regime buckets have tens of rows, not thousands;
        # ddof=0 understates uncertainty and makes spike gates too eager.
        "std": float(sorted_values.std(ddof=1)) if len(sorted_values) > 1 else 0.0,
        "min": float(sorted_values.min()),
        "p05": q(0.05),
        "p10": q(0.10),
        "p25": p25,
        "p50": p50,
        "p75": p75,
        "p90": q(0.90),
        "p95": q(0.95),
        "max": max_value,
        "iqr": float(p75 - p25),
        "max_minus_median": float(max_value - p50),
        "top_5pct_mean": top_tail_mean(0.05),
        "top_10pct_mean": top_tail_mean(0.10),
    }
    if include_sum:
        out["sum"] = float(sorted_values.sum())
    if unit is not None:
        out["unit"] = unit
    return out
```

---

DSR, PBO/CSCV, FDR/Romano-Wolf, Calmar/Sortino/Ulcer/Rolling Vol,

Metrics:
Sharpe, Calmar, Return, Drawdown, Profit Factor, Trades Number, Win Rate, what else?

Robustness:
within_plateau, param_neighbors_avg_sharpe, neighbor_consistency, neighbor_count, what else?

cost_sensitivity:
method (fast_estimate_from_persisted_trade_costs, etc..)
sharpe_after_2x_fees
sharpe_after_3x_slippage
break_even_fee_rate
break_even_fee_rate_pct
"interpretation": "Estimated from persisted trade costs; PROVE slippage stress is still the final authority."

out_of_sample_preview:
available (true, false)
first_80pct_sharpe
last_20pct_sharpe
decay_ratio
"interpretation": "Quick time-split preview only; full WFO remains the validation gate.",
"suggestion": "Decay ratio >= 0.85 supports refinement; <0.70 is a reject-or-redesign warning."

gates passed:
min_trades
min_sharpe
min_pf
max_dd
neighbor_consistency
cost_resilience




- what else we MUST track per each grid combo results? what is "must have" what is "should have" what is "nice to have" and what "optional"?

---

Why did not we discuss tracking RR values per gird combo?

---

Why did you say nothing yet about MAE/MFE? it's key metrics! it allows us to analyze what to optimize! Do we need MAE/MFE distribution per each individual grid combo (maybe not all combos, but top-N combos, like top 10/20/40 grid combos, configurable)? in which format? is this format below good?
```
 "mfe": {
      "count": 89,
      "sum": 4.71644,
      "avg": 0.05299370786516854,
      "median": 0.03186,
      "p25": 0.013197,
      "p75": 0.066718,
      "p90": 0.1417402,
      "p95": 0.1663763999999998,
      "min": 0.000155,
      "max": 0.305978
    },
    "mae": {
      "count": 89,
      "sum": -2.654041,
      "avg": -0.029820685393258425,
      "median": -0.022386,
      "p25": -0.039883,
      "p75": -0.011813,
      "p90": -0.0038735999999999987,
      "p95": -0.001997200000000001,
      "min": -0.125254,
      "max": -0.00074
    },
    "r_multiple": {
      "count": 89,
      "sum": 99.7384,
      "avg": 1.120656179775281,
      "median": -0.348,
      "p25": -1.3763,
      "p75": 1.7774,
      "p90": 5.617720000000002,
      "p95": 8.766039999999995,
      "min": -2.8897,
      "max": 33.3628
    },
  "mfe_distribution": {
      "le_0.0": 0,
      "0.0_to_0.01": 18,
      "0.01_to_0.03": 23,
      "0.03_to_0.05": 18,
      "0.05_to_0.10": 17,
      "gt_0.10": 13
    },
    "mae_distribution": {
      "lt_-0.10": 2,
      "-0.10_to_-0.05": 13,
      "-0.05_to_-0.03": 21,
      "-0.03_to_-0.01": 35,
      "-0.01_to_0.0": 18,
      "ge_0.0": 0
    },
    "r_multiple_distribution": {
      "<-2R": 10,
      "-2--1R": 22,
      "-1-0R": 18,
      "0-1R": 9,
      "1-2R": 10,
      "2-5R": 9,
      "5R+": 11
    },
 "mfe_mae": {
      "avg_mfe_pct": 0.05299370786516854,
      "avg_mae_pct": -0.029820685393258425,
      "avg_r_multiple": 1.120656179775281,
      "avg_exit_efficiency": -1.047760227272727,
      "interpretation": "Exit efficiency is weak: trades move favorably but give back too much before exit.",
      "suggestion": "Scatter is mixed. Tune stops and filters in a small local grid, then compare zone shares rather than only Sharpe. Winning trades needed up to 2.32R adverse movement; either widen volatility stops or improve entry timing. Prefer volatility-based stops over round-number stops."
    },
```



or do we also need top-N trades results for each grid combo with their indivisual trade anslysis? or is that overkill?

What else did we miss what is usable? We MUST know everything possible at this level!

---


FLOX has these indicators implemented:

#include "flox/indicator/atr.h"
#include "flox/indicator/autocorrelation.h"
#include "flox/indicator/bollinger.h"
#include "flox/indicator/cci.h"
#include "flox/indicator/correlation.h"
#include "flox/indicator/dema.h"
#include "flox/indicator/ema.h"
#include "flox/indicator/kama.h"
#include "flox/indicator/kurtosis.h"
#include "flox/indicator/macd.h"
#include "flox/indicator/parkinson_vol.h"
#include "flox/indicator/rma.h"
#include "flox/indicator/rogers_satchell_vol.h"
#include "flox/indicator/rolling_zscore.h"
#include "flox/indicator/rsi.h"
#include "flox/indicator/shannon_entropy.h"
#include "flox/indicator/skewness.h"
#include "flox/indicator/slope.h"
#include "flox/indicator/sma.h"
#include "flox/indicator/stochastic.h"

For what and how can we use all these metrics? Per grid combo, per whole Grid all combos overall resutls?
Which other indicators and metrics do may we need and for what? Which we must have which are good to have and which are optional/nice to have?



---

I provide some C++ source files and tests which are most related to our backtesting process, so you can see their core logic, so you can understand and analyze the pipeline and see the extension points for new result stats and metrics which are not yet implemented and also indicators which we need or may need in the future which are not yet implemented:

vendor/flox/_my/crush-glm/docs-log/walk_forward.cpp
vendor/flox/_my/crush-glm/docs-log/grid_search.cpp
vendor/flox/_my/crush-glm/docs-log/backtest_runner.cpp
vendor/flox/_my/crush-glm/docs-log/backtest_result.cpp
vendor/flox/_my/crush-glm/docs-log/simulated_executor.cpp

vendor/flox/_my/crush-glm/docs-log/test_autocorrelation.cpp
vendor/flox/_my/crush-glm/docs-log/test_backtest_metrics.cpp
vendor/flox/_my/crush-glm/docs-log/test_backtest_queue.cpp
vendor/flox/_my/crush-glm/docs-log/test_backtest_slippage.cpp
vendor/flox/_my/crush-glm/docs-log/test_backtest.cpp
vendor/flox/_my/crush-glm/docs-log/test_capi_backtest.cpp
vendor/flox/_my/crush-glm/docs-log/test_grid_search.cpp
vendor/flox/_my/crush-glm/docs-log/test_indicators.cpp
vendor/flox/_my/crush-glm/docs-log/test_multi_symbol_strategy.cpp
vendor/flox/_my/crush-glm/docs-log/test_multi_tf_alignment.cpp
vendor/flox/_my/crush-glm/docs-log/test_optimization_stats copy.cpp
vendor/flox/_my/crush-glm/docs-log/test_optimization_stats.cpp

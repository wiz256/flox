 beofre running on all 40 coins, what do you think about:

1) reducing 5 folds to smaller folds like 2-3?
  - Fold 0: IS=29 trades, OOS=8 trades, TrainSh=17.1, TestSh=11.8, Deg=0.69 → PASS
  - Fold 1: IS=20 trades, OOS=4 trades (< 5 min) → SKIP (OOS too few)
  - Fold 2: IS=28 trades, OOS=4 trades (< 5 min) → SKIP
  - Fold 3: IS=34 trades, OOS=10 trades, TrainSh=17.0, TestSh=37.6 → PASS
  - Fold 4: IS=30 trades, OOS=1 trade (< 5 min) → SKIP
we still have TOO SMALL amount of trades to decide! we maybe missing some good strategies just because too small count of trades in a fold! but the strategy may
be still good

2) if possible, I would include into reporting not only the final score, but also the parts from which the score consist of:
 double score = 0.25 * nSharpe
                 + 0.20 * nCalmar
                 + 0.15 * nPF
                 + 0.15 * nPlateau
                 + 0.10 * nTrades
                 + 0.10 * nCost
                 + 0.05 * nWinRate
                 - ddPenalty;
it would really usable to see ALL of them in reporting to decide what is best

3) I don't like that we use in code and in reporting the short names for parameters! I would keep the full names in both reporting and in toString and inside C++
 code! it allows to have much less cognitive time to analyze and follow the code!